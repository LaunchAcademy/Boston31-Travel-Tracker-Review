{
  "type": "module",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "build:client": "yarn workspace travel-tracker-client build",
    "dev": "yarn workspace travel-tracker-server dev",
    "dev:debug": "yarn workspace travel-tracker-server dev:debug",
    "dev:server:debug": "PORT=4000 yarn workspace travel-tracker-server dev:debug",
    "dev:server": "PORT=4000 yarn workspace travel-tracker-server dev",
    "dev:client": "yarn workspace travel-tracker-client dev",
    "heroku-postbuild": "yarn workspace travel-tracker-client build",
    "start": "yarn workspace travel-tracker-server start"
  },
  "engines": {
    "node": "^16.18"
  }
}
